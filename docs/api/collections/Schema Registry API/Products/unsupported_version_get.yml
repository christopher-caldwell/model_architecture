info:
  name: "[Unsupported Version] Product"
  type: http
  seq: 2
  tags:
    - Products

http:
  method: GET
  url: "{{BASE_URL}}/products?v=1.0.1"
  params:
    - name: v
      value: 1.0.1
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 422 Response
    description: Unknown schema version
    request:
      url: "{{BASE_URL}}/products"
      method: GET
      params:
        - name: v
          value: 99.0.0
          type: query
    response:
      status: 422
      statusText: Unprocessable Entity
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "error": ""
          }
