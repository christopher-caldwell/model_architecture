info:
  name: "[Unsupported Version] Order"
  type: http
  seq: 2
  tags:
    - Orders

http:
  method: GET
  url: "{{BASE_URL}}/orders?v=4.0.2"
  params:
    - name: v
      value: 4.0.2
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 422 Response
    description: Unknown schema version
    request:
      url: "{{BASE_URL}}/orders"
      method: GET
      params:
        - name: v
          value: 99.0.0
          type: query
    response:
      status: 422
      statusText: Unprocessable Entity
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "error": ""
          }
