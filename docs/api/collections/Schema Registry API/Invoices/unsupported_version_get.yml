info:
  name: "[Unsupported Version] Invoice"
  type: http
  seq: 2
  tags:
    - Invoices

http:
  method: GET
  url: "{{BASE_URL}}/invoices?v=0.8.0"
  params:
    - name: v
      value: 0.8.0
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 422 Response
    description: Unknown schema version
    request:
      url: "{{BASE_URL}}/invoices"
      method: GET
      params:
        - name: v
          value: 99.0.0
          type: query
    response:
      status: 422
      statusText: Unprocessable Entity
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "error": ""
          }
