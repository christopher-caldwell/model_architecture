info:
  name: Create Book
  type: graphql
  seq: 4

graphql:
  method: POST
  url: "{{BASE_URL}}/graphql"
  body:
    query: |-
      mutation CreateBook($input: CreateCatalogTitleInput!) {
        createBook(input: $input) {
          isbn
          dtCreated
          dtModified
          title
          authorName
        }
      }
    variables: |-
      {
        "input": {
          "isbn": "9780132350884",
          "title": "Clean Code",
          "authorName": "Robert C. Martin"
        }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
